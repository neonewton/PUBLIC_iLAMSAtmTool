# copy and paste to terminal or powershell

#MacOS

open folder with Terminal
python3 -m venv venv
source venv/bin/activate

pip install --upgrade pip
pip3 install -r requirements.txt

streamlit run Home.py

ctrl + C to kill the connection